{
  "name": "learn-care-chart",
  "version": "0.1.0",
  "private": true,
  "description": "LearnCare Chart public data contest mockup",
  "scripts": {
    "build": "node scripts/build.js",
    "start": "npx serve dist",
    "dev": "npx serve ."
  },
  "engines": {
    "node": ">=20"
  }
}
